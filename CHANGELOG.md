# 更新日志

[English](CHANGELOG.en.md)

这里只记录用户能感受到的变化；实现细节见 Git 提交。

## Unreleased

- **本地构建不会再把一个早已从源码里删掉的插件打进包。** setuptools 的 `build/lib` 缓存从不删文件，所以一个曾经放在源码树里的插件，在 git 早就不认识它之后，仍会被之后每一次本地构建重新打进 wheel——如果那是一个私有集群的连接器，它就会随包装到别人机器上。现在打包校验会核对插件集合与源码一致，多出来的直接失败并告诉你清掉缓存重建。（GitHub 上发布的产物不受影响：CI 在干净环境里 checkout，没有这份缓存。）

## 2.1.0 - 2026-08-28

**升级一次就是升级完：macOS 上只剩一份后端，重装之后正在跑的控制面会自己换过来；发布也收成推一个 tag。**

- **macOS App 不再自带第二份后端，装 CLI 就是装后端。** 此前 App 里打包了一整份 Python 运行时，打开过 App 之后后台服务就被钉死在那一份上；你 `uv tool install --force .` 只换了磁盘上的包，正在跑的进程仍是 App 里的旧代码，改完后端怎么重装都不生效。现在 App 只是界面。**如果你从未单独装过 CLI**，升级后 App 会明确告诉你装哪条命令，而不是默默起不来。顺带的好处是构建 App 从分钟级降到几秒。
- **重装之后控制面会自己换到新版本。** 此前判断「进程是不是旧的」只看一份手工维护的能力清单，同一个版本号配着旧进程可以长期共存而没有任何提示。现在版本或能力对不上就重启。
- **MCP 连上一个还没重启的旧控制面时会明说。** 以前新工具打到旧接口上只会得到一个普通 HTTP 错误，看不出是该升级；现在会直接告诉你两边各是什么版本、缺哪些能力、该重启控制面。
- **发布只需要推一个 tag。** 推 tag 之后自动创建 GitHub Release，Windows 压缩包照常上传。而且如果版本号已经改了、更新日志也已经封了条目，却忘了打 tag，CI 会直接失败并告诉你下一步——`2.0.0` 就曾经这样停在半路：提交推了、CI 全绿，但没有 tag 也没有 Release，谁也发现不了。
- **`serverpilot doctor` 会列出各处的版本。** 控制面、本机 CLI、MCP 入口，以及每台服务器的 collector；对不上会点名说下一步该重启控制面还是重装某台机器的 collector。此前只能靠人逐台去想。
- **服务器采集入口开始上报自己的版本。** 旧脚本不带这个字段照常工作，不会因为升级期间版本不齐就拒绝分配。
- **装错版本的本机插件不再无声消失。** 停留在旧协议的插件此前会在发现阶段被静默跳过，看起来像没装；现在会作为明确的错误出现在 `doctor` 里。
- **历史图不再把同一张卡画两遍。** 换过 GPU 的服务器（例如容器重建）会在库里留下旧的显卡记录，历史接口把它们一并返回，于是 8 张卡的机器画出 16 条曲线、图例里每张卡出现两次。现在只返回机器上当前真实存在的卡，所选时间段内没有数据的曲线也不再占一个图例位。
- **服务器详情页不再又空又长。** 服务器组卡片此前把每个字段竖着堆成一列，右边大半幅宽度整片空着；现在与主机卡片用同一套排布横向铺满，长路径和说明单独占整行。
- **两个容易读反的卡数改了名字。** 此前并排显示「一次最多 4 卡」和「单次租约上限 8 卡」，看起来自相矛盾。现在分别叫「现在可申请」（此刻这台机器还能拿到多少，会变）和「单次上限」（一次申请最多能拿多少，固定）；两者相同时只显示一个，上限未知时不编造数字。
- **新增升级检查清单。** 把那些无法自动完成、漏了会静默出错的步骤写在一处：确认控制面进程真的换了版本、在客户端里重连 MCP 才会刷新工具列表、重装 Agent 全局规则、逐台升级服务器上的采集入口与占卡程序。
- **本机控制面活着时，`serverpilot daemon status` 会报 live，不再被系统代理或 `HTTP_PROXY` 说成死了。** CLI、MCP 和 Windows 桌面桥接访问 127.0.0.1 时不再走环境代理或操作系统代理；macOS 上即使进程环境里没有代理变量，Python 仍会读系统配置里的 HTTP 代理，探测以前会被它拦下，于是 curl 能通、命令行却说服务没起来。
- **`gpu_status(server_id=…)` 收窄到一台服务器时，不再带上你没问的分组，也不会把那些分组写成「一次最多 0 卡」。** 此前「没有逐卡库存的分组也要出现」把这次没问到的裸机分组也投影进去，成员一空就把上限聚合成 0；Agent 和桌面端会据此以为那个集群没有容量。委托集群在不收窄时仍然出现——它有成员，只是成员没有逐卡行。组里真的一张空闲卡都没有时，`largest_allocatable_block` 仍是 0。

## 2.0.0 - 2026-08-28

**源码收成日常产品：本机 daemon、桌面 App、五个 MCP 工具；浏览器界面和调度器/规划面已经拿掉，你一直在用的申请闭环还在。而且只有一种集群——插件接入的集群不再是平行概念，它和裸机分组用同一组字段描述，于是同一个实验终于能在两个集群之间比较。**

- **日常 MCP 申请签名为 `gpu_apply(server_group_id?, server_id?, gpu_count=1, task?)`。** `gpu_count` 来自启动脚本或配置中的任务并行度，安全默认 1，绝不从空闲容量推断——一台八卡机器上的单卡任务因此只会拿到一张卡。
- **`gpu_status` 按 组 → 服务器 → SKU 投影可申请容量（`name` / `vram_mib` / `total_count` / `available_count`），不再列出每张空闲卡。**
- **分组携带工作目录、环境说明和数据/权重说明。** 成员服务器可继承或覆盖工作目录。环境说明只供阅读，不会被执行或注入。
- **插件接入的集群（如 Slurm）以正常分组出现在 `gpu_status` 里。** 带组名、共享工作目录、卡型和 SSH，形状与裸机分组一致；没有平行的 `scheduler_servers` 顶层桶。此前这类集群只有一行扁平记录，你在 App 里给它起的名字和设的工作目录会被整个丢掉，Agent 连它叫什么都不知道。
- **每个集群都带一份同形的申请约束。** 租约是到显式释放还是到点硬杀、最长多久、一次最多几卡、每卡配多少 CPU 和内存、申请最长要等多久、会不会排队。这些以前只写在插件源码里，Agent 看不到——一个三小时的训练放到一小时硬杀的集群上，会在第 60 分钟被杀掉。
- **「一次能拿到的最大卡数」和「池子里还剩多少」是两个数。** 一个跨节点还剩 27 张卡的分区，如果作业必须落在单节点，很可能一张 8 卡都开不出来。拿不准时显示「按需申请」而不给数字，绝不用剩余总数冒充上限。
- **日常申请先选定分组，再由调度器在组内 best-fit 一台主机。** 插件接入的分组同样可以直接选组申请。`server_id` 仍可用于未分组主机。
- **去掉了外部调度器提交（调度目标、作业、传输及其 MCP 工具）、通用资源规划、工作负载预设，以及整套浏览器 `/ui` 页面。** 这些不是 Agent 日常拿卡的路径；`gpu_status → gpu_apply → gpu_release` 仍在。Slurm 一类集群改走本机插件（随包 `slurm-immediate`），不再有一套平行的调度器代码。
- **日常 MCP 现在正好五个工具。** 上面三个，加上 `gpu_add_server` 和 `gpu_update_server`。删除服务器只在 App 和 REST，不是 MCP 工具，该服务器还有进行中租约时会拒绝。
- **浏览器界面已移除。** 人看状态、纠错、删服务器走 macOS 或 Windows 桌面 App，读的仍是同一份本机快照。
- **桌面 App 不再把插件集群显示成「CPU 节点」，分组标题也不再写「无 GPU」。** 现在写「按需申请 · 一次最多 N 卡」，详情里能看到租约时限这类申请约束。
- **桌面 App 详情页去掉了重复信息。** 主机卡片只保留列表上没有的事实，组名和工作目录各自只出现一处，历史图的多卡颜色图例整个面板只画一份。
- **插件作者注意：插件协议为 `schema_version 3`，停留在 v2 的插件不会被发现。** `info` 必须声明 `limits`。随包的 `slurm-immediate` 已是 v3。

## 1.9.1 - 2026-08-27

**ServerPilot 1.9.1 让新增的 MCP 入口面板在常规安装下真正可用。**

- **`uv tool install` 装出来的设置页能找到 MCP 入口了。** 这是 macOS 上最常见的形态。查找时先解析了解释器的符号链接再找同目录文件，于是跳出了存放 `serverpilot-mcp` 的目录、落到基础解释器自己的目录里。daemon 由 launchd 启动、没有自己的 PATH，所以也没有别的途径能找到它。
- **数据库备份在 Windows 上可用了。** 每个 SQLite 连接现在都会在发布备份前显式关闭——此前用作 context manager 只结束事务、不关闭连接，而 Windows 不允许替换仍有句柄的文件；刷盘也改走可写句柄，因为 Windows 不能对只读打开的文件做 fsync，连接关掉之后备份仍会在那里失败。
- **真正提交成功的 Slurm 作业不会再被报成失败。** `sbatch` 在提前退出的路径上不会读完 stdin，于是给它喂脚本的进程被信号杀掉，而这个信号成了整条管道的退出码，盖掉了 sbatch 自己的判定：配额被拒、分区写错都变成没有意义的 141；而作业其实已被接受时，它会继续在集群里跑，本地却没有任何东西指向它。
- **从 App 复制出来的 `mcpServers` 配置里，可执行文件路径按原样显示。** 不再把每个斜杠转义成 `\/`。
- **GPU 行如果标记自己不可申请、状态文字却仍自称可用，会重新被拒绝。** 此前只按状态码判断，状态不属于可分配集合时这种矛盾就漏了过去。

## 1.9.0 - 2026-08-27

**ServerPilot 1.9.0 让 Agent 能从 Windows 机器接入，也能读懂它返回的每一个字；分配器不再把你的集群一张一张拆散。**

- **接入 Agent 只要一条命令。** `serverpilot mcp install --client codex|claude|cursor` 走各客户端自己的注册机制，Cursor 合并进 `~/.cursor/mcp.json`，不动已有的其他 server。想自己粘贴就用 `serverpilot mcp config`，只打印不写盘。README 和 Agent 指南现在也给出标准的 `mcpServers` 配置块。
- **桌面 App 的设置页现在会显示本机 MCP 入口的绝对路径和可直接粘贴的 `mcpServers` 配置。** 复制后即可交给 Codex、Claude 或 Cursor，不必自己拼命令。找不到可执行文件时，同一处会说明原因并给出安装提示。
- **Windows 压缩包里，`serverpilot-mcp.exe` 就放在 App 旁边。** 此前下载下来的只有 GUI，机器上根本不存在 MCP 入口，文档里的注册命令只对源码安装有效。
- **MCP 说明、三个工具的描述、ServerPilot 返回的每一个值，现在都是英文。** 可申请的卡报 `available`；被占用的卡报稳定的原因码，如 `running`、`held_idle`、`busy_unmanaged`。桌面 App 仍是中文——两面各自独立，改一面不会牵动另一面。
- **`gpu_apply` 在能独立满足整份申请的服务器里，挑可用卡数最少的那台；一份租约始终只落在一台机器上。** 这决定着集群是一直好用，还是被切出满地的洞：此前按服务器名字母序取卡，一张单卡申请会落在排在最前的八卡机上，来几次这样的申请，就再没有一台机器起得了八卡任务。八卡申请也可能拿到两台机器上的 5+3——单机任务没法用，能用这八张卡的人也拿不到。
- **`no_capacity` 现在作为结构化数据返回，不再是一条报错文本。** Agent 能分清「集群满了」和「连接断了」，不会把一个答复当故障反复重试。释放一个已经释放的租约会得到确认，而不是报错。
- **Slurm / LSF / PBS 集群用本机插件接入，不再当裸机登记。** 插件只登记你自己作业里的卡；申请前，`gpu_status` 以 `scheduler_servers` 报告集群余量，申请成功后真实 GPU 身份才出现，空闲回收会取消对应作业。随包附带 Slurm 参考插件 `slurm-immediate`。
- **集群拒绝你，不再像集群只是满了。** 配额被拒、调度器不可达、插件自身出错，都会带着原因作为失败返回，而不是报成 `no_capacity` 让 Agent 干等。
- **修了两种集群作业被丢着不管的情况。** 卡已分配之后，ServerPilot 不会再让插件重复申请一次；租约跨多台机器时，每个作业都会记账，释放时不留残留。申请和释放集群资源也不再卡住其他操作——此前本机其他写操作最长要等一分钟。
- **空闲回收时取消不掉的集群作业，现在会写进审计轨迹。** 以前它会无声消失：作业还在跑、继续占着你的配额，本地却已经没有任何东西指向它。你自己释放租约时仍然直接拒绝，不会把没做成的事报成成功。
- **控制面不在线时，`serverpilot keepalive inspect` 和 `serverpilot keepalive stop` 能查看并停掉还在占卡的 worker，`serverpilot daemon reclaim` 收回端口。** 三条都不依赖 daemon 在线。端口归属校验失败时，提示会直接指出占用进程和它的命令行，不再只说端口不归自己。
- **MCP 握手时上报的是 ServerPilot 自己的版本，不再是 MCP SDK 的版本。** 每个工具也声明了自己的读写语义，客户端能分清只读查询和租约变更，不必把三个工具一视同仁地拦截。
- **Windows 独立 App 又能建数据库了。** 打包版此前漏带了数据库迁移脚本，解压后只有源码部署一条路能用。
- **发布会拦下一个装得上、用起来才坏的 wheel。** tag 与包版本对不上，或 wheel 里缺迁移脚本、内置插件没带可执行位时，构建直接失败——插件少了这个位不会报错，只会在发现阶段静默消失。
- **新增 `serverpilot --version`。**
- **安全文档与实现一致了。** 控制面没有认证；ServerPilot 管理自己的占卡进程和插件侧分配，但永远不碰你的任务；fail-closed 准入信任 SSH 用户与远端采集入口。控制面停止后，占卡 worker 会继续占卡，直到控制面恢复后 reconcile，或有人在服务器上停掉它们。
- **本地 daemon 一次慢请求或卡住，不再堵住同一条连接上的其他 MCP 工具。** 每个工具也给出真正的入参 schema，不再是自由参数。

## 1.8.0 - 2026-08-24

**ServerPilot 1.8.0 让 `gpu_status` 只在占用确实属于调用者的卡上给遥测：空闲卡只讲容量，自己的租约给逐卡利用率和拖后腿的那张。**

- **Agent 看到的 `gpu_status` 分成三组，各自回答一个问题。** 可申请卡只讲容量（型号、显存、可用），忙卡只讲被谁占着，遥测只出现在自己租到的卡上。此前空闲卡也带遥测，而空闲卡上能观测到的负载全部来自 ServerPilot 自己的占卡程序（按显存 80% 持有，真正分配前才停止）——一台八卡全空的机器因此读起来像装满了八成，据此核对可用性的 Agent 会判定没有卡可用。
- **新增 `gpu_status(lease_id=…)`。** 返回自己这张租约的逐卡近 10 分钟均值与当前采样，外加整份租约的汇总——平均利用率、全租约最小可用显存、多卡时的利用率落差和最慢的那张卡。这是"我的任务把卡用好了没有、还能不能加 batch、有没有哪张卡拖后腿"要看的数字；此前自己租到的卡只会掉进紧凑的 `busy_gpus`，除了任务名什么都没有。
- **`gpu_status` 不再接受 `include_busy`。** 忙卡始终以 `busy_gpus` 返回任务名，自己的卡用 `lease_id` 看。可申请卡的状态统一为「可用」，不再区分占卡的内部变体。八卡机器的一次 `gpu_status` 响应从 5,957 字节降到 1,749 字节。

## 1.7.0 - 2026-08-23

**ServerPilot 1.7.0 把服务器页做回一张可以逐列比较的表，四项资源指标一律带压力柱；空闲回收精确到单张显卡。**

- **服务器页是一张表，一台机器一行 44 pt。** `GPU 利用率 / 显存占用率 / CPU 负载 / 内存占用率` 四项一律「百分比 + 压力柱」，等宽等权，可以顺着一列往下比。此前 CPU 与内存只有数字没有柱，而排序栏又允许按 CPU 负载排序——排出来的顺序在界面上找不到依据。
- **窗口变窄时表格从右折叠列，而不是换成另一种布局。** 完整 SSH 指令在任何宽度下都不再被截断，四条压力柱在任何宽度下都不折叠。1280 收起 `GPU 配置`，更窄再收起 `项目 / 任务`，这两项在行的 tooltip 和详情弹窗里都还在。
- **列头就是排序控件。** 点一次按该列排序，再点一次反转方向，当前列加深并带箭头。列头现在也有辅助功能名称，屏幕阅读器不再读到一排没有名字的按钮。
- **表格行不再打印「无运行任务」这类否定事实。** 无 GPU 的主机把型号列换成核数与内存总量，那才是这台机器的身份。
- **空闲回收精确到单张显卡。** 申请了 8 张只用 1 张时，其余 7 张各自计时、到期后单独归还，仍在跑的那张不受影响。此前只要有一张卡在工作，整份申请里的空闲卡都会被一起保住。
- **CPU 核数、内存总量、最高温度、显存合计（绝对 GB）和远端工作区完整路径移入详情弹窗新增的`主机`卡片。** 工作目录此前在行里只能截成 `…workspace/tmp/user`，截断后不携带任何信息；这些事实也仍在行的 tooltip 里。
- **使用情况页与设置页改用与服务器页相同的卡片语言。** 一组事实装进一张白色卡片，行与行之间只用发丝线分隔，不再各自带底色和描边。使用情况详情新增`资源合计`，设置页新增`数据状态`（连接、快照新鲜度与修订号、服务器 / GPU / 租约计数、能否执行资源变更）。
- **服务器详情弹窗不再使用半透明材质，改为与其它页面同一层平面。** 逐卡网格的最小列宽提到能容下全部内容，`4 / 8…`、`32 / …`、`任务：…` 这类词中截断消失。
- **开启系统「增强对比度」时界面真正跟随。** 卡片补上描边、发丝线加深、状态色重解到 7:1、进度柱底槽加深，切换后立即生效，不需重启。
- **设置页卡片改为靠左对齐，不再在宽窗口里居中。** 只读测试夹具下`连接`不再谎称已连上本机服务。
- **使用情况页与服务器详情弹窗现在逐项可读。** 此前把整页折叠成一个元素，屏幕阅读器只能读到一句概述，读不到里面的任何按钮和数值。
- **状态配色重做为两层。** 深色记号层（`#00832F` / `#B05A00` / `#E40021`）用于色点、进度柱和状态词，浅色区域层（`#E7F8EB` / `#FFF1E5` / `#FFE7E8`）承担亮度。此前为了让小色点自身达标而整体压暗，导致绿色发闷、琥珀发土。
- **界面平面从三层收为两层（白色内容面 + `#E9ECF1` 页面底）。** 原先三层每层只差 1.06–1.09，看起来像渲染瑕疵而不是层次。
- **字阶加入 26pt 展示档，界面上终于有元素能当主角。** 最大最小之比从 1.70 提到 2.60；所有数字改为等宽数字，刷新时不再抖动。
- **全界面字号统一为六档 Apple 语义字阶。** 原先 19 档、含半像素，界面因此开始跟随系统文字大小缩放。
- **设置页去掉与侧栏、页面标题重复的第三个「设置」标题。** 筛选控件的重复标签不再在宽窗口下竖排断行。

## 1.6.0 - 2026-08-21

**ServerPilot 1.6.0 让 Agent 读取 GPU 状态省下约七成上下文，并让被占着空转的显卡自动还回来。**

- **Agent 查看 GPU 更省上下文。** 连接方式和远端工作目录现在按服务器返回一次，不再逐张显卡重复；`gpu_status` 默认还会列出占用中的显卡和它们的任务名，所以判断“有没有空卡、被谁占着”一次调用就够，不必再查第二次。在一台 8 卡服务器上，这条判断路径的返回量从约 21,800 字符降到约 6,700 字符。
- **`gpu_status` 新增 `server_id` 参数。** 只关心一台服务器时可以直接收窄返回。
- **`gpu_release` 现在回显被释放的租约编号和终态。** 持有多个租约时可以逐个确认，不会因为释放了一个就误以为全部完成。
- **桌面 App 刷新更轻。** 它不再拉取自己从不显示的通用资源与外部 scheduler 投影，实测每次刷新的状态载荷从约 77,000 字符降到约 59,700（−22.4%）。服务器、GPU、租约、资源用量等界面真正渲染的内容逐字节不变。
- **桌面端三处文案与图标回到设计契约。** 服务器列表头图标改为 `server.rack`，设置项统一为「数据采集间隔」（与「刷新」区分开，后者只是重读本机状态），使用情况页空状态改为「当前没有资源分配。」。
- **状态色对比度达标。** 正常绿与注意琥珀加深到 `#339653` 与 `#AA7C00`（只改明度、保持色相），状态点和资源进度柱对内容表面与页面底色的对比度从最低 1.53 提到不低于 3.01，满足非文本图形的无障碍阈值；错误红原本已达标，保持不变。
- **空闲占用的 GPU 会自动还回来。** 租约的显卡在采集能看见的前提下持续没有任何计算进程，先记一条警告，继续空闲则自动释放并重新进入可申请池。Agent 忘记释放、或任务跑完没收尾，都不会再让显卡一直被锁住；采集中断期间不计时，不会误收正在跑但暂时看不见的任务。
- **显卡状态不再一律显示「任务使用中」。** 现在区分「任务占用」（真的在跑）、「占卡」（已申领但没观察到任务）、「未归属占用」和「归属冲突」，一眼能看出哪些卡是被占着空转的。
- **升级须知：外部 Agent 的全局规则是磁盘上的静态副本。** 升级后请重跑 `python3 scripts/install_agent_policy.py all --install`（Cursor 用 `--print` 后手动粘贴），否则规则描述与实际返回不一致。

## 1.5.12 - 2026-08-17

**ServerPilot 1.5.12 可从本机控制面删除服务器，并收掉无法使用的暂停/恢复与网页预约提交入口。**

- **可在「编辑或移除服务器」中从本机控制面删除服务器。** 旧本机服务缺少该能力时会切换到内置后端；删除后不会因 YAML 清单在重启时复活。有进行中的租约或资源分配时会被拒绝，且不会停止远端进程。
- **移除从未展示的旧桌面界面，以及无法使用的暂停/恢复入口和网页预约/维护提交表单。** 预约与维护仍可只读查看。

## 1.5.11 - 2026-08-17

**ServerPilot 1.5.11 将空闲占卡提高到每张卡约 80% 显存与利用率，并让长任务在 worker 换代后仍保持正确归属。**

- **空闲占卡按每张卡当前 CUDA 可见总显存占用约 80%，同时把 GPU 利用率拉到约 80%。**
- **多 GPU 长任务在 worker 重启、子进程替换或分阶段接续时，会继续显示为“任务使用中”。** 不再因为 PID 变化把整笔任务错误标为“归属待确认”。
- **升级后的服务会清理旧版本留下的工作任务归属错误。** 占卡 helper 的真实身份异常仍明确显示为错误，不会被误当作普通任务。
- **桌面端会展示任务指派与进程观测更新。** 并把清理操作明确限制为任务结束后，不会停止远端任务。

## 1.5.10 - 2026-08-15

**ServerPilot 1.5.10 修复 Windows Release 的跨平台校验，使 Windows x64 桌面 App 能由 GitHub 自动构建并作为可下载资产发布。**

- **Windows 构建校验现在同时兼容 Windows 与 POSIX 路径分隔符。** Release 只在 Windows Runner 成功完成桌面 UI、WebView2 主机和打包检查后上传压缩包。

## 1.5.9 - 2026-08-15

**ServerPilot 1.5.9 为 Windows 用户提供与 macOS 版保持同一资源工作流的完整桌面 App。**

- **Windows 版使用系统 WebView2 打开独立桌面窗口，不再跳转外部浏览器。** 总览、搜索、筛选、表头排序、资源申请、添加服务器、占卡设置和采集间隔都通过受限的本机桥接调用同一份 loopback 控制面。
- **服务器表铺满窗口的可用宽度。** GPU 配置靠左，GPU 利用率、显存占用率、CPU 负载和内存占用率按最近 10 分钟均值展示和排序。
- **Windows 服务器详情沿用 macOS 的逐 GPU 显存环和空闲 / 占卡 / 繁忙 / 错误标签。** 并以 2×2 历史图展示 CPU、内存、GPU 利用率和显存占用率。
- **GitHub Release 会由 Windows Runner 生成并附上 `ServerPilot-*-windows-x64.zip`。** 无需 Windows 用户自行安装 Python 或 uv。

## 1.5.8 - 2026-08-15

**ServerPilot 1.5.8 让服务器资源总览与桌面详情页以更稳定、可读的方式呈现 CPU、内存与逐 GPU 状态。**

- **服务器总览的 GPU 利用率、显存占用率、CPU 负载和内存占用率统一使用最近 10 分钟观测均值。** 端点快照新增主机侧 `host_telemetry.recent_average`，旧本机服务不会被误当作兼容版本。
- **资源表会随可用窗口填满布局。** 项目 / 当前任务列吸收余宽；GPU 配置靠左，四个资源列使用完整专业表头并按同一 10 分钟口径排序。
- **服务器详情将逐 GPU 信息恢复为紧凑横向卡片。** 每张 GPU 的环图显示当前显存占用，状态以小标签明确表示空闲、占卡、繁忙或错误；资源历史固定为 2×2 图表布局。

## 1.5.7 - 2026-08-15

**ServerPilot 1.5.7 让 GPU 与纯 CPU 服务器的资源状态以同一份实时快照一致呈现。**

- **`gpu_status` 为每张 GPU 返回最近观测和近 10 分钟的显存、GPU/显存控制器利用率与温度均值。** 同时给出当前可见卡的汇总，便于区分瞬时波动和持续负载。
- **GUI 与 MCP 共用 daemon 的 REST 快照，不会分别 SSH 采集。** GPU 详情页直接显示同一份逐卡均值。
- **新增服务器首次只读采集会识别 GPU、纯 CPU 或尚未确认。** 已确认的纯 CPU 服务器保留 CPU/内存监控，在 GUI 和 `gpu_status.cpu_only_servers` 中明确标注，但永不成为 GPU 分配目标。

## 1.5.6 - 2026-08-15

**ServerPilot 1.5.6 修复了任务归还后自身占卡 worker 被误报为“任务使用中”的问题。**

- **Agent 的日常合约不变。** 任务结束仍只需调用 `gpu_release`；ServerPilot 自行恢复空闲占卡，Agent 不需要关闭占卡开关。
- **占卡进程重启、daemon 重启或短暂控制面丢失后，不会再把 ServerPilot 自己的 worker 当作外部任务。** helper 会对它自己登记的 v3 worker 作只读身份证明，并证明目标卡唯一的 driver-visible PID；ServerPilot 仅在该证明与新鲜采集的 PID/boot identity 一致时重新登记 worker。
- **证明不一致、状态损坏或额外业务进程仍保持 fail closed，绝不自动收养。** Agent 会得到“占卡校验失败，暂不可申请”，而不是误导性的“任务使用中”。
- **已确认停止的 keeper 会清除旧进程身份。** 避免下一次启动拿旧 PID 误比对新 worker。

## 1.5.5 - 2026-08-14

**ServerPilot 1.5.5 将占卡协议升级到 v3，并收紧不同 GPU/驱动环境中的设备选择与控制面可靠性。**

- **占卡 adapter 启停前先执行只读 `--protocol-info` 预检。** 确认 v3、pidfd identity 与 PCI bus ID 能力；预检不通过时返回 `keepalive_helper_incompatible`，不发送 mutation。
- **占卡 wire/state 使用 v3 与 `workers.v3.json`。** 旧 v2 payload/state 不读取、不删除、不停止，直接 fail closed。

- **`gpu_index` 只保留服务器显示编号。** collector schema v2 另按 PCI bus 生成 `cuda_ordinal`。`gpu_apply` 返回 `cuda_device_order=PCI_BUS_ID`、租约 ordinal 集合和逐卡 ordinal，不再把 GPU UUID 当作 `CUDA_VISIBLE_DEVICES`。
- **没有当前 CUDA ordinal 的 GPU 不参与分配。** 旧 collector schema 与旧 PID-only 占卡状态直接 fail closed，不再收养或降级处理。
- **占卡 worker 持久保存 PID、Linux boot ID、进程启动 ticks 与固定 marker。** 停止时通过 pidfd 锁定进程。即使目标 Python 没有 pidfd 包装，仍调用 Linux pidfd syscall，不退回普通 PID signal。
- **修正请求体实际限流与断连转发、并发限流、Web CSRF、CSV 字段投影和 SQLite 原子备份。** 普通 Agent 改派继续受 lease owner 限制，App operator 使用独立纠错路由。
- **CPU/内存 admission 同时计入 direct GPU commitments 与 generic host claims。** routine MCP 的传输重试使用进程内调用命名空间，不会把后续同参数申请折叠为旧 lease。

## 1.5.3 - 2026-08-14

**ServerPilot 1.5.3 让 Agent 可以直接连接服务器，同时不再混淆工作目录和代码路径。**

- **routine `gpu_status` 与 `gpu_apply` 返回结构化 `ssh {host, port, user}`。** Agent 在申请成功后直接 SSH 执行 workload，不再把“未加入 Codex saved hosts”误报成服务器不可达。
- **`workspace_path` 是 SSH 后操作所在的远端工作目录，不是代码仓库路径。** Agent 先进入该目录，再在其下进行命令、代码同步与产物操作。
- **新增机器可读的 `workspace {path, kind=working_directory, use_as_cwd=true, code_location=not_provided}`。** 将连接参数、工作目录、代码位置和 CUDA selector 分成四个独立概念；保留 `workspace_path` 兼容字段。
- **Agent 规则明确区分正常 SSH 执行与绕过 ServerPilot 查卡、选卡、申请或释放 GPU。**

## 1.5.2 - 2026-08-13

**ServerPilot 1.5.2 清理了“归属待确认”的误报，并恢复 App 的人工纠错能力。**

- **任务正常重启不再误报归属冲突。** routine Agent 的旧进程已经全部退出、且完整采集确认每张租用 GPU 都出现同一轮新进程时，ServerPilot 自动更新观察归属；新进程只有稳定出现才会触发冲突，完整采集确认重试间隙为空时会保留租约并清除瞬态冲突；旧新进程稳定并存、不完整采集和高级显式 binding 仍然 fail closed。
- **历史错误会真正结束。** 租约释放或失去活动资源后，`lease_process_conflict` 与 `orphaned_busy` 告警立即关闭；升级启动与 reconcile 会清理旧版本遗留的陈旧告警。
- **App 状态与 broker 保持一致。** 原生 App 直接采用 `desired / actual / publicly_available / public_status`，不可申请的卡不再显示“可用”，`desired=ON, actual=OFF` 明确显示“占卡未运行”。
- **人类可以释放孤立的 Agent 租约。** App 使用独立的 operator 纠错路由释放确认已结束的任务；日常 Agent 仍只能释放自己的 lease。

## 1.5.1 - 2026-08-13

**ServerPilot 1.5.1 让 Agent 更容易把 GPU 租约正确转换为远端单卡或多卡工作负载。**

- **单卡进程不再需要猜 selector。** `gpu_apply` 保留既有 `cuda_visible_devices` 完整集合，并在 `gpus[]` 每项新增 `gpu_cuda_visible_devices` 单 UUID，兼容原有多卡调用的同时支持每卡一个进程。
- **远端工作区边界更清楚。** `workspace_path` 明确是所选服务器上的路径；Agent 要先通过当前获授权的远端端点进入，不会再把它当成本地 Codex worktree 路径。
- **运行时失败更快让出 GPU。** 日常契约要求 workload 前先用返回的 selector 做最小 CUDA 初始化；失败立即释放，并在当前任务中记住不兼容的服务器，避免立即重试同一组合。
- **无容量和传输失败不再触发无效轮询。** `no_capacity` 留到下一 turn 或后续工作周期；transport 失败最多重试一次，且不会被误报为容量不足。
- **并行任务的释放责任更明确。** 申请者显式跟踪每个 `lease_id`，即使把执行交给子任务，也要等到每个 lease 明确返回 `released=true`。

## 1.5.0 - 2026-08-12

**ServerPilot 1.5.0 把日常 GPU 协调和原生监控收敛为更直接的工作流。**

- **空闲占卡按 GPU 独立管理。** endpoint 只保留一个持久开关，ServerPilot 为每张合格空闲卡维护独立 worker；Agent 申请时只让选中的占卡进程退出，不影响同机其他 GPU。
- **没有容量就明确失败。** 裸机 GPU 申请只会立即返回 lease 或 `no_capacity`，不再产生隐式等待队列；Agent 只使用返回的 endpoint、GPU 与 CUDA selector。
- **项目可以声明资源卡。** `.serverpilot/resource-card.json` 为项目保存可校验的 direct-GPU 预设合同，减少 Agent 根据任务名或当前空闲量猜配置。
- **App 聚焦日常监控。** 原生界面收敛为“服务器 / 使用情况 / 设置”，统一展示服务器资源、当前项目与任务、逐卡状态和历史趋势。
- **归属、采集和占卡继续 fail closed。** stale、未知、非托管进程、冲突或维护中的 GPU 不会被投影为可申请资源。

## 1.4.0 - 2026-08-10

**ServerPilot 1.4.0 建立了统一资源控制面和可独立运行的原生桌面体验。**

- **GPU、CPU、内存和外部调度目标共享资源合同。** 服务端统一计算 capacity、used、claimed 与 available；GUI、CLI 和 MCP 不再各自推算可用量。
- **服务器采集扩展为受限脚本协议。** `server-script-v1` 只允许固定入口输出受校验的只读快照；脚本缺失、超限或格式错误都会安全闭锁。
- **原生 App 提供完整资源总览。** 可搜索、筛选和排序服务器，并按 endpoint 查看 CPU、内存、GPU 利用率和显存的 1h / 6h / 24h 历史。
- **后台服务与 App 生命周期分离。** 用户级 daemon 长期持有状态，关闭 GUI 不会停止控制面；App 内置后端和迁移，可完成独立构建与验证。
- **资源归属与外部调度边界更清楚。** 项目、Agent、任务、空 lease 和队列使用同一投影；Slurm 通过独立受限 adapter 接入，不伪装成普通 GPU 服务器。

## 1.3.0 - 2026-08-10

**ServerPilot 1.3.0 移除了面向特定服务器或集群的硬编码。**

- **Endpoint 使用固定 `observation_profile`，外部 scheduler 使用受限的 transport / inspection profile。**
- **本机管理员把 profile 映射到可信绝对路径 wrapper。** API、App 和 MCP 不能提交任意 shell、argv 或环境变量。
- **未知或未配置 profile 一律 fail closed。** 旧 scheduler 命令配置升级后会停用，等待管理员重新选择安全 profile。
- **文档和运行契约改为通用 adapter 模型，不再依赖单个集群名称。**

## 1.2.0 - 2026-08-10

**GPU Broker 正式更名为 ServerPilot。**

- **GitHub 仓库、macOS / Windows App、Web UI、文档和公共 API 标题统一使用 ServerPilot。**
- **新增 `serverpilot` 与 `serverpilot-mcp` 命令入口。**
- **保留旧 `gpu-broker`、`gpu-broker-mcp`、`GPU_BROKER_*`、daemon identity 和数据目录兼容入口。** 升级不丢失 inventory、历史、租约或 MCP 配置。
- **`/api/v1/state` 与既有调度语义保持兼容。**

## 1.1.0 - 2026-08-10

**GPU Broker 1.1.0 扩展了服务器遥测和原生资源监控。**

- **新增封闭的 observation / scheduler adapter 边界。** 没有开放第二套认证或远程命令控制面。
- **CPU、内存、GPU、claim 与 availability 使用 fail-closed 资源投影。** CPU-only endpoint 不再伪造 GPU 容量。
- **保存受限的 endpoint 遥测历史，并按稳定 GPU UUID 展示逐卡序列。**
- **macOS 资源页加入 CPU、内存、GPU 利用率和 GPU 显存图表。** 按需读取历史并降低悬停渲染开销。
- **`/api/v1/state` 继续作为分配状态的权威快照。** 既有 lease、CLI、MCP 和 Slurm 语义保持兼容。

## 1.0.0 - 2026-08-06

**GPU Broker 首个稳定版本。**

- **App、CLI 与 MCP 统一读取权威的 `/api/v1/state` 快照。**
- **macOS 原生 App 以稳定 ID 协调服务器、项目和资源状态。** 删除服务器后相关页面同步更新。
- **revision 与资源使用来自同一份已提交控制面快照。** 减少跨页面和跨客户端的数据分叉。
- **loopback REST 与领域服务成为唯一公共业务路径。**
